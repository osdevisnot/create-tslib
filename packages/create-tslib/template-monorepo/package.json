{
  "name": "template-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "template-monorepo",
  "keywords": [
    "template-monorepo"
  ],
  "repository": "username/template-monorepo",
  "license": "MIT",
  "author": "username <useremail>",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "coverage": "tslib-cli coverage",
    "format": "lerna run format",
    "postinstall": "npm run sort",
    "lint": "lerna run lint",
    "prepare": "npm run bootstrap",
    "prepublishOnly": "npm run lint && npm run coverage",
    "presetup": "git clean -fdX",
    "setup": "pnpm install || npm install",
    "postsetup": "npm run sort",
    "sort": "sort-package-json",
    "postsort": "lerna exec sort-package-json",
    "test": "tslib-cli test",
    "updates": "ncu -u",
    "postupdates": "lerna exec 'ncu -u'",
    "preversion": "npm run lint",
    "postversion": "git push --follow-tags"
  },
  "prettier": "@osdevisnot/prettier",
  "devDependencies": {
    "@osdevisnot/prettier": "0.2.2",
    "@osdevisnot/tslib-cli": "<version>",
    "@types/jest": "24.0.17",
    "@types/node": "12.7.1",
    "jest": "24.8.0",
    "lerna": "3.16.4",
    "prettier": "1.18.2",
    "rollup": "1.19.4",
    "ts-jest": "24.0.2",
    "tslint": "5.18.0",
    "typescript": "3.5.3"
  }
}
